<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:background="@drawable/fons"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- TextView para mostrar el puntaje -->
    <TextView
        android:id="@+id/tvScore"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Score: 0"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:layout_margin="16dp"
        android:background="#80000000"
        android:padding="8dp" />

    <!-- Vista personalizada VistaJoc que representará el juego -->
    <com.example.practica_ninja.VistaJoc
        android:id="@+id/vistaJoc"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" /> <!-- Esto hace que la vista ocupe el espacio restante -->

</LinearLayout>
